<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColor">@color/blacklighter</item>
        <item name="android:itemBackground">@color/nicebackground</item>
        <item name="android:colorBackground">@color/nicebackground</item>
        <item name="android:popupMenuStyle">@style/myPopupMenuStyle</item>
    </style>


    <!---->

    <!--POPUP MENU STYLE-->
    <style name="myPopupMenuStyle" parent="@android:style/Widget.Holo.Light.PopupMenu">
    </style>

    <style name="ListActivity" parent="Theme.AppCompat.Light.DarkActionBar"/>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <!--THEME OF TOOLBAR-->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" >
    <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="EditTextLightGray" parent="AppTheme">
    <item name="colorControlNormal">@color/lightgray</item>
    <item name="colorControlActivated">@color/lightgray</item>
    <item name="colorControlHighlight">@color/lightgray</item>
    </style>

    <style name="FragmentNiceBackground">
        <item name="android:colorBackground">@color/nicebackground</item>
        <item name="android:background">@color/nicebackground</item>
    </style>

</resources>
